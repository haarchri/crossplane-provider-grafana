---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: oncalloncallshifts.grafana.jet.crossplane.io
spec:
  group: grafana.jet.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - grafanajet
    kind: OncallOnCallShift
    listKind: OncallOnCallShiftList
    plural: oncalloncallshifts
    singular: oncalloncallshift
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OncallOnCallShift is the Schema for the OncallOnCallShifts API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: OncallOnCallShiftSpec defines the desired state of OncallOnCallShift
            properties:
              deletionPolicy:
                default: Delete
                description: DeletionPolicy specifies what will happen to the underlying
                  external when this managed resource is deleted - either "Delete"
                  or "Orphan" the external resource.
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  byDay:
                    description: This parameter takes a list of days in iCal format.
                      Can be MO, TU, WE, TH, FR, SA, SU
                    items:
                      type: string
                    type: array
                  byMonth:
                    description: This parameter takes a list of months. Valid values
                      are 1 to 12
                    items:
                      type: number
                    type: array
                  byMonthday:
                    description: This parameter takes a list of days of the month.  Valid
                      values are 1 to 31 or -31 to -1
                    items:
                      type: number
                    type: array
                  duration:
                    description: The duration of the event.
                    type: number
                  frequency:
                    description: The frequency of the event. Can be daily, weekly,
                      monthly
                    type: string
                  interval:
                    description: The positive integer representing at which intervals
                      the recurrence rule repeats.
                    type: number
                  level:
                    description: The priority level. The higher the value, the higher
                      the priority.
                    type: number
                  name:
                    description: The shift's name.
                    type: string
                  rollingUsers:
                    description: The list of lists with on-call users (for rolling_users
                      event type)
                    items:
                      items:
                        type: string
                      type: array
                    type: array
                  start:
                    description: The start time of the on-call shift. This parameter
                      takes a date format as yyyy-MM-dd'T'HH:mm:ss (for example "2020-09-05T08:00:00")
                    type: string
                  startRotationFromUserIndex:
                    description: The index of the list of users in rolling_users,
                      from which on-call rotation starts.
                    type: number
                  teamId:
                    description: The ID of the OnCall team. To get one, create a team
                      in Grafana, and navigate to the OnCall plugin (to sync the team
                      with OnCall). You can then get the ID using the `grafana_oncall_team`
                      datasource.
                    type: string
                  timeZone:
                    description: The shift's timezone.  Overrides schedule's timezone.
                    type: string
                  type:
                    description: The shift's type. Can be rolling_users, recurrent_event,
                      single_event
                    type: string
                  users:
                    description: The list of on-call users (for single_event and recurrent_event
                      event type).
                    items:
                      type: string
                    type: array
                  weekStart:
                    description: Start day of the week in iCal format. Can be MO,
                      TU, WE, TH, FR, SA, SU
                    type: string
                required:
                - duration
                - name
                - start
                - type
                type: object
              providerConfigRef:
                default:
                  name: default
                description: ProviderConfigReference specifies how the provider that
                  will be used to create, observe, update, and delete this managed
                  resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - name
                type: object
              providerRef:
                description: 'ProviderReference specifies the provider that will be
                  used to create, observe, update, and delete this managed resource.
                  Deprecated: Please use ProviderConfigReference, i.e. `providerConfigRef`'
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace
                  and name of a Secret to which any connection details for this managed
                  resource should be written. Connection details frequently include
                  the endpoint, username, and password required to connect to the
                  managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: OncallOnCallShiftStatus defines the observed state of OncallOnCallShift.
            properties:
              atProvider:
                properties:
                  id:
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's
                        last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition
                        type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
