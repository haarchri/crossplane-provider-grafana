apiVersion: grafana.jet.crossplane.io/v1alpha1
kind: CloudStack
metadata:
  name: my-stack
spec:
  forProvider:
    slug: 'crossplanetest'
    name: 'crossplanetest'
    regionSlug: "us"
  providerConfigRef:
    name: grafana-cloud-provider